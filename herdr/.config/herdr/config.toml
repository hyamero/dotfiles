onboarding = false
[theme]
name = "rose-pine"
auto_switch = false

[keys]
# Chord, not a printable char: a ':' prefix eats ':w' in vim (-> workspace_picker),
# and 'esc' eats esc+hjkl (-> focus_pane_*). ctrl+space transmits as NUL.
prefix = "ctrl+space"
